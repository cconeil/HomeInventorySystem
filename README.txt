This application is used to keep inventory sorted for insurance purposes.  It was built using Java and Swing.  It has it's own format for input and export that is a text file.  You can use the add modify and sort methods to manipulate data.

This function was written November 2012